class partical {
  constructor (x, y) {
    this.r = Math.random() * 255 | 0
    this.g = Math.random() * 255 | 0
    this.b = Math.random * 255 | 0
    this.x = x
    this.y = y
  }
}
export default partical